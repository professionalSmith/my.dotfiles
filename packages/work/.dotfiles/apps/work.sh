# General aliases
alias work='cd ~/work.repos'

# Repository aliases
alias gitops='cd ~/work.repos/gitops && code .'
alias imports-service='cd ~/work.repos/imports-service && code .'
alias my-jobs='cd ~/work.repos/my-jobs && code .'
alias prompt-based-search='cd ~/work.repos/prompt-based-search && code .'
